<div class="container">
  <% if @posts.count == 0 %>
    <h3 class="mt-3 text-center">検索結果がありません。</h3>
  <% else %>
    <div class="row mt-3">
     <p>検索ワード：「<%= @tag %>」 検索結果：<%= @posts.count %> 件</p>
    </div>
    <%= render 'list', posts: @posts %>
  <% end %>
</div>